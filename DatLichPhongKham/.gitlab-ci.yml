stages:
  - build
  - test
  - deploy

variables:
  DOCKER_DRIVER: overlay2
  COMPOSE_PROJECT_NAME: datlichphongkham
  # ... (giữ nguyên các variables khác của bạn)

# Job Build
build_job:
  stage: build
  tags:
    - local
  variables:
    GIT_STRATEGY: clone
    GIT_DEPTH: 0
  before_script:
    - echo "===== CHECKING ENVIRONMENT ====="
    - pwd
    - echo "===== LISTING ROOT FILES ====="
    - Get-ChildItem -Force | Select-Object Name, Length
    - echo "===== SEARCHING FOR POM.XML ====="
    - $pomPath = Get-ChildItem -Recurse -Filter pom.xml -ErrorAction SilentlyContinue | Select-Object -First 1 -ExpandProperty FullName
    - if ($pomPath) {
        Write-Output "✅ Found pom.xml at: $pomPath";
        $pomDir = Split-Path -Parent $pomPath;
        Write-Output "📁 pom.xml directory: $pomDir";
        Write-Output "📁 Current directory: $(Get-Location)";
        if ($pomDir -ne (Get-Location).Path) {
          Write-Output "🔄 Changing to pom.xml directory...";
          Set-Location $pomDir;
          Write-Output "✅ Changed to: $(Get-Location)";
        }
      } else {
        Write-Output "❌ pom.xml NOT FOUND anywhere!";
        Write-Output "Checking git files...";
        git ls-files | Select-String pom
        Write-Output "Checking git status...";
        git status
        Write-Output "Forcing git checkout...";
        git checkout -f HEAD
        git reset --hard HEAD
        $pomPath = Get-ChildItem -Recurse -Filter pom.xml -ErrorAction SilentlyContinue | Select-Object -First 1 -ExpandProperty FullName
        if ($pomPath) {
          Write-Output "✅ Found pom.xml after checkout at: $pomPath";
          $pomDir = Split-Path -Parent $pomPath;
          Set-Location $pomDir;
        } else {
          Write-Output "❌ pom.xml still NOT FOUND after checkout!";
          exit 1;
        }
      }
    - echo "===== VERIFYING POM.XML ====="
    - if (Test-Path pom.xml) {
        Write-Output "✅ pom.xml verified at: $(Resolve-Path pom.xml)";
        Get-Item pom.xml | Select-Object FullName, Length
      } else {
        Write-Output "❌ pom.xml verification failed!";
        exit 1;
      }
  script:
    - echo "===== BUILD PROJECT ====="
    - echo "Current directory: $(Get-Location)"
    - echo "POM file: $(Resolve-Path pom.xml)"
    - mvn clean package -DskipTests
  artifacts:
    paths:
      - "target/*.jar"
    expire_in: 1 hour

# Job Test
test_job:
  stage: test
  tags:
    - local
  before_script:
    - $pomPath = Get-ChildItem -Recurse -Filter pom.xml -ErrorAction SilentlyContinue | Select-Object -First 1 -ExpandProperty FullName
    - if ($pomPath) {
        $pomDir = Split-Path -Parent $pomPath;
        if ($pomDir -ne (Get-Location).Path) {
          Set-Location $pomDir;
        }
      }
  script:
    - echo "===== RUN TESTS ====="
    - mvn test

# Job Deploy
deploy_job:
  stage: deploy
  tags:
    - local
  only:
    - main
  script:
    - echo "===== RESTARTING CONTAINERS ====="
    - docker-compose down
    - docker-compose up -d --build
    - Start-Sleep -Seconds 10
    - docker ps -a